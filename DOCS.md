# ТКП·Про — документация (прототип итерации А.3 + А.6)

Рабочий прототип двух пунктов дорожной карты направления
«А. Конструктор и мастер подбора»: **А.3 «УЗИП»** и **А.6 «Работы и ППО»**.
Стек: React + Vite + Tailwind v4, расчётное ядро `utils.ts`, справочник
`data/catalog.ts`, мастер `components/Wizard.tsx`, оболочка `App.tsx`.

## 1. Что реализовано в этой итерации

### А.3 — УЗИП (замечания заказчика закрыты)

- **Все классы по IEC 61643-11 / СП 256.1325800** — отдельными строками
  опросника: класс I (тип 1, 10/350), класс II (тип 2, 8/20),
  класс I+II (комбинированный), класс III (тип 3, тонкая защита).
  Каждая строка — количество комплектов, с пояснением зоны защиты (LPZ).
- **Резервный автомат добавляется автоматически**: по одному `uzp-backup`
  (автомат резервной защиты УЗИП 3P C63) на каждый комплект силового УЗИП —
  требование производителей; счётчик и сумма пересчитываются вживую
  (бейдж `anim-pop`).
- **Слаботочные УЗИП перенесены в шаг «ПЛК и модули»**: RS-485, Ethernet и
  каналы 24 В DC закладываются вместе с защищаемыми интерфейсами (секция
  «Защита интерфейсов и каналов»). Из шага УЗИП — заметная ссылка-переход
  «Перейти к ПЛК».
- **Позиции — из справочника** (`catalog.ts`): `uzp-t1`, `uzp-t2`, `uzp-t12`,
  `uzp-t3`, `uzp-backup`, `uzp-rs485`, `uzp-eth`, `uzp-io` — ничего не зашито
  в страницу.

### А.6 — Работы и ППО (единообразный вид)

- Три ровные строки `CountRow`: сборка / проектирование / ППО, каждая с
  подписью ставки тарифа.
- Тёмная панель «Стоимость работ»: себестоимость по ставкам, наценка,
  итог в продаже — пересчёт на каждое изменение с «перескоком» суммы
  (`anim-pop`).
- Тумблер «отдельной строкой в документе».

## 2. Расчётное ядро

`calcProject`: база наценки = закупочная; работы = часы × ставки тарифов
(сборка/проект/ППО), в продаже — с наценкой `workMarkup`; итог с НДС.
Часы работ мастер передаёт в шкаф (`hours`/`designHours`/`softwareHours`).

## 3. Запуск

```powershell
npm install
npm run dev      # http://localhost:3000
npm run build    # производственная сборка в dist/
```

Проверка итерации:
1. Открыть мастер → шаг 4 «УЗИП»: поставить класс II = 1 → бейдж
   «Резервный автомат: 1 шт» и его сумма; поставить I+II = 1 → станет 2 шт.
2. Нажать «Перейти к ПЛК» → секция «Защита интерфейсов и каналов» с тремя
   счётчиками (RS-485 / Ethernet / 24 В DC); позиции попадают в состав.
3. Шаг 12 «Работы и ППО»: ровные строки, справа живой расчёт (10 ч сборки ×
   1800 ₽ + … → сумма в продаже); итог проекта в карточке перескакивает.
4. «Добавить в проект» → шкаф и ЗИП появляются в структуре; резервный
   автомат отмечен зелёной пометкой.

## 4. Дорожная карта (статус по основной ветке)

**Сделано**: авторизация и роли (JWT + серверная матрица), онлайн-режим в
локальной сети (автодетекция, офлайн-очередь, «корзина» справочника 90 дней,
реквизиты по учётной записи), мастер подбора 14 шагов, интерактивный макет
дверцы (А.4), измерительные приборы WB/ТТ (А.5), контракт дат unix-мс,
секционирование (фронтенд), составные шкафы CQE N / ПРОВЕНТО.

**Осталось — по направлениям**

- **А. Конструктор**: А.1 переработка шага «Корпус» (производители из БД,
  сборка «стенка к стенке» отдельным шагом, навесное оборудование по
  документации ПРОВЕНТО); А.2 «Ввод и линии» (вводной АВ по схеме АВР,
  отходящие вручную с аксессуарами, учёт по счётчикам и ТТ); ~~А.3 УЗИП~~ ✅
  (прототип); ~~А.4 макет дверцы~~ ✅; ~~А.5 приборы~~ ✅; ~~А.6 Работы~~ ✅
  (прототип).
- **Б. Шкафы и графика**: конфигуратор пустых/преднаполненных шкафов с
  заказным шифром; расчёт тепловыделений для микроклимата; общий вид шкафа
  по ГОСТ; соединение «дверь к задней стенке» (≤ 2 корпусов).
- **В. Аналитика**: отдельная админ-панель (пользователи, справочник +
  «корзина», статистика по направлениям и производителям); регистрация
  времени подготовки ТКП (только админ).
- **Г. Документы**: однолинейная схема АВР приложением к ТКП; серверный PDF.
- **Д. Инфраструктура**: персист секционирования (EF-миграция), парсеры
  прайсов, код-сплиттинг.

## 5. Журнал изменений

- **`feature/wizard-uzip-work`** — прототип А.3 + А.6: УЗИП всех классов из
  справочника с автоматическим резервным автоматом и переносом слаботочных
  УЗИП в шаг «ПЛК»; шаг «Работы и ППО» в единых строках CountRow с живым
  расчётом по тарифам. Файлы: `catalog.ts`, `components/Wizard.tsx`,
  `components/ui.tsx`, `App.tsx`, `types.ts`, `utils.ts`, `DOCS.md`.
